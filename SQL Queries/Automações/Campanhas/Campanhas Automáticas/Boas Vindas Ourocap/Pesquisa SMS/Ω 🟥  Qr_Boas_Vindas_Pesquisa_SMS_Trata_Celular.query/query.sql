SELECT
  A.CPF_CNPJ,
  A.ID_PROPOSTA,
  '55' + REPLACE(REPLACE(REPLACE(REPLACE(A.CELULAR, '(', ''), ')', ''), ' ', ''), '-', '') AS CELULAR
FROM TB_BOAS_VINDAS_PESQUISA_SMS A
WHERE CELULAR IS NOT NULL